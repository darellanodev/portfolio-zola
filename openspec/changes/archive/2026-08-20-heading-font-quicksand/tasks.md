## 1. Google Fonts Link

- [x] 1.1 Update Google Fonts `<link>` in `templates/partials/header.html` from `Fredoka:wght@500;600;700` to `Quicksand:wght@500;600;700`

## 2. CSS Variable

- [x] 2.1 Update `--heading-font` in `sass/main.scss` from `'Fredoka', sans-serif` to `'Quicksand', sans-serif`

## 3. Documentation

- [x] 3.1 Add Typography section to `DESIGN.md` documenting Quicksand (headings, 500/600/700) and Inter (body, 400/600)

## 4. Verification

- [x] 4.1 Run `zola build` or `zola serve` and verify the compiled CSS contains the correct font-family values
