# No spec changes

This change is an implementation-level CSS fix. No new capabilities are added and no existing capability requirements are modified. The existing `featured-projects-section` and `projects-cards-layout` specs already describe the correct card layout behavior; this fix corrects a CSS implementation bug that prevented that behavior from rendering correctly.
