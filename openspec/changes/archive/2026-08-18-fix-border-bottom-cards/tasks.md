## 1. CSS Fix

- [x] 1.1 Remove the `&.bottom-divider { border-bottom: none; }` block from `.featured-list .bloglist-row` in `sass/parts/_posts_list.scss`

## 2. Verification

- [x] 2.1 Reload the home page and confirm all 4 project cards show a complete border on all sides
- [x] 2.2 Reload `/projects/` and confirm all project cards show a complete border on all sides
- [x] 2.3 Verify the blog listing page (non-featured) still applies `bottom-divider` correctly (single-column stacked list)
